import { useState } from 'react'
import Head from 'next/head'
import DropDown from '../components/DropDown'
import Graph from '../components/Graph'
import { H1, Paragraph } from '../components/Typography'
import { Municipality } from '../utils/types'
import { data, pledges, paris } from '../data/stockholm'

type PropsType = {
  municipalities: Array<Municipality>
}

const Home: React.FC<PropsType> = ({ municipalities }: PropsType) => {
  const municipalitiesName = municipalities.map((item) => item.Name)
  const [currentStep, setCurrentStep] = useState('now')

  return (
    <>
      <Head>
        <title>Klimatkollen</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Graph
        width={500}
        height={250}
        currentStep={currentStep}
        data={data}
        paris={paris}
        pledges={pledges}
      />
      <button onClick={() => setCurrentStep('now')}>step1</button>
      <button onClick={() => setCurrentStep('paris')}>step2</button>
      <button onClick={() => setCurrentStep('pledges')}>step3</button>
      <button onClick={() => setCurrentStep('summary')}>step4</button>
      <H1>Klimatkollen</H1>
      <Paragraph>
        Låt oss ta tempen på hur väl din kommun når upp till de mål som är uppsatta i
        parisavtalet.
      </Paragraph>
      <DropDown municipalitiesName={municipalitiesName} />
    </>
  )
}

export async function getServerSideProps() {
  const municipalities = await fetch(
    'http://klimatkollen.vercel.app/api/municipalities',
  ).then((res) => res.json())
  return {
    props: { municipalities },
  }
}

export default Home
